<p id="notice"><%= notice %></p>

<h1>Devises</h1>

<table>
  <thead>
    <tr>
      <th>Pincode</th>
      <th>Volume</th>
      <th>Temperatura</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @devises.each do |devise| %>
      <tr>
        <td><%= devise.pincode %></td>
        <td><%= devise.volume %></td>
        <td><%= devise.temperatura %></td>
        <td><%= devise.status %></td>
        <td><%= link_to 'Show', devise %></td>
        <td><%= link_to 'Edit', edit_devise_path(devise) %></td>
        <td><%= link_to 'Destroy', devise, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Devise', new_devise_path %>
